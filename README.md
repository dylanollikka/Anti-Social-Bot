# Robot Artificial Intellegence

Light sensitive robot written in Open-cv/ C using MISO model

Uses the following Microprocessers:
  - Arduino UNO: runs c code, master in
  - Raspberry pi 3: runs open-cv, slave out
